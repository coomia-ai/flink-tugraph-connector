# Normalize line endings: LF in the repo, native on checkout.
* text=auto eol=lf

# Windows scripts must stay CRLF.
*.bat text eol=crlf

# The POSIX wrapper must stay LF and executable.
gradlew text eol=lf

# Binary assets.
*.jar binary
